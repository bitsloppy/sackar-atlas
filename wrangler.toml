# Deployment is handled by GitHub Actions — see .github/workflows/deploy.yml
# The actual deploy runs from site/dist/server/ using the adapter-generated wrangler.json
# with: npx wrangler deploy --name sackar-atlas --compatibility-flags nodejs_compat
#
# This file is kept for local wrangler dev / reference only.

name = "sackar-atlas"
compatibility_date = "2026-04-15"
compatibility_flags = ["nodejs_compat"]

[vars]
PUBLIC_KEYSTATIC_GITHUB_APP_SLUG = "sackar-atlas-cms"
KEYSTATIC_GITHUB_CLIENT_ID = "Iv23liEzLlXB3JL9B5wN"
